const commandSearch=document.querySelector('#command-search');const categoryFilter=document.querySelector('#category-filter');function filterCommands(){const q=(commandSearch?.value||'').trim().toLowerCase();const cat=categoryFilter?.value||'';document.querySelectorAll('.command-tile').forEach(tile=>{const okTitle=!q||tile.dataset.title.includes(q);const okCat=!cat||tile.dataset.category===cat;tile.hidden=!(okTitle&&okCat);});}commandSearch?.addEventListener('input',filterCommands);commandSearch?.addEventListener('keydown',event=>{if(event.key==='Enter'){event.preventDefault();filterCommands();}});categoryFilter?.addEventListener('change',filterCommands);const componentSearch=document.querySelector('#component-search');function filterComponents(){const q=(componentSearch?.value||'').toLowerCase();document.querySelectorAll('.component-buttons a').forEach(link=>{link.hidden=q&&!link.textContent.toLowerCase().includes(q);});}componentSearch?.addEventListener('input',filterComponents);